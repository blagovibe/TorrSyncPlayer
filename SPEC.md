# TorrSyncPlayer - Спецификация проекта

## 1. Обзор проекта

**TorrSyncPlayer** — десктопное приложение для потокового воспроизведения видео через WebTorrent с синхронизацией между устройствами.

### Ключевые возможности
- Потоковое воспроизведение видео через WebTorrent
- Синхронизация воспроизведения между несколькими клиентами
- PeerJS signaling для координации пиров
- Децентрализованная архитектура (P2P)

## 2. Архитектура

### Компоненты
1. **Tauri Desktop App** — клиентское приложение (React + Rust)
2. **Signaling** — текущая реализация клиента использует PeerJS для broker/signaling; in-repo сервер не требуется для локальной сборки клиента
3. **WebTorrent** — библиотека для P2P стриминга

### Схема взаимодействия
```
[Client A] <---> [PeerJS signaling broker] <---> [Client B]
    |                                            |
    +------------ P2P (WebRTC) -----------------+
    |                                            |
[WebTorrent Seed]                              [Video Playback]
```

## 3. UI/UX Спецификация

### Окна
- **Главное окно**: видеоплеер + элементы управления
- **Окно настроек**: параметры подключения

### Цветовая схема
- Фон: `#1a1a2e` (тёмно-синий)
- Акцент: `#e94560` (красный)
- Текст: `#eaeaea`
- Вторичный фон: `#16213e`

### Компоненты UI
- Плеер с кастомными контролами
- URL-бар для ввода magnet-ссылок
- Статус-индикатор (пиры, загрузка)
- Кнопки: Play/Pause, Sync, Fullscreen

## 4. Функциональная спецификация

### 4.1 Воспроизведение видео
- Поддержка форматов: MP4, WebM, MKV (через браузерный движок)
- Буферизация при загрузке с WebTorrent
- Адаптивный битрейт (если доступно)

### 4.2 Синхронизация
- Один клиент — "ведущий" (master), остальные — "ведомые" (slaves)
- Master передаёт состояние: timestamp, play/pause, seek
- Latency compensation для корректировки

### 4.3 Сигналинг
- Текущий runtime использует PeerJS-сигналинг из `client/src/services/P2PService.ts`
- Обмен данными синхронизации идёт через WebRTC data channel после установки P2P-соединения
- Room code в UI соответствует PeerJS peer id хоста
- Будущий self-hosted signaling server должен быть Go-модулем в `server/`, если проект вернётся к собственному WebSocket signaling

## 5. Технический стек

- **Frontend**: React 18 + TypeScript + Vite
- **Desktop**: Tauri 2.x (Rust)
- **Signaling**: PeerJS в текущем клиенте; Go-сервер зарезервирован как будущий self-hosted вариант
- **P2P**: WebTorrent + WebRTC
- **Styling**: CSS

## 6. Критерии приёмки

- [ ] Приложение запускается без ошибок
- [ ] Воспроизводится видео из magnet-ссылки
- [ ] Несколько клиентов синхронизируют воспроизведение
- [ ] Интерфейс соответствует цветовой схеме
- [ ] Сборка работает для Linux (и Windows/macOS)

## 7. Roadmap

1. **Phase 1**: Базовая структура проекта, Tauri + React
2. **Phase 2**: Интеграция WebTorrent, видеоплеер
3. **Phase 3**: Сигналинг-сервер на Go
4. **Phase 4**: Синхронизация между клиентами
5. **Phase 5**: UI/UX полировка, релиз
