name: Build and Release TorrSyncPlayer

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  NODE_VERSION: 22

jobs:
  checks:
    name: Client checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Unit tests
        run: npm run test

      - name: Dependency audit
        run: npm audit --audit-level=high

      - name: Web build
        run: npm run build

  build:
    name: Electron ${{ matrix.artifact_name }}
    needs: checks
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: TorrSyncPlayer-Linux-AppImage
            build_command: npm run electron:build:linux
            artifact_path: client/release/*.AppImage
          - os: windows-latest
            artifact_name: TorrSyncPlayer-Windows
            build_command: npm run electron:build:win
            artifact_path: client/release/*.exe
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Electron artifact
        env:
          ELECTRON_BUILDER_WINDOWS_CODE_SIGNING_DISABLED: "true"
        run: |
          echo "=== Checking prerequisites ==="
          ls -la dist/index.html electron/main.cjs
          echo "=== npm_execpath: $npm_execpath ==="
          echo "=== Running build ==="
          ${{ matrix.build_command }}
        shell: bash

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error

  release:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine release tag
        run: |
          REF="${{ github.ref }}"
          if [[ "$REF" == refs/tags/v* ]]; then
            RELEASE_TAG="${REF#refs/tags/}"
          else
            RELEASE_TAG="push-${{ github.run_id }}-${{ github.run_attempt }}"
          fi
          echo "RELEASE_TAG=$RELEASE_TAG" >> "$GITHUB_ENV"

      - name: Create release tag (non-version-tag only)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "$RELEASE_TAG" -m "Automated release for ${{ github.sha }}"
          git push origin "$RELEASE_TAG"

      - name: Download AppImage artifact
        uses: actions/download-artifact@v4
        with:
          name: TorrSyncPlayer-Linux-AppImage
          path: artifacts/linux

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: TorrSyncPlayer-Windows
          path: artifacts/windows

      - name: Publish GitHub release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_TAG }}
          generate_release_notes: true
          draft: true
          files: |
            artifacts/linux/*.AppImage
            artifacts/windows/*.exe
